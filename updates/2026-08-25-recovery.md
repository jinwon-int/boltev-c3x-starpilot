# 2026-08-25 — C3X post-format recovery and canonical readback

## Scope

Record the completed StarPilot/AGNOS/network recovery and the 2026-08-25 live readback. This entry distinguishes software/config projection from the still-pending first ignition vehicle validation.

## Recovery result

| 항목 | 결과 |
|---|---|
| Device | Comma 3X `comma-dbba2a27` |
| Tailscale | new identity `tizi-the-galaxy`, `100.90.4.121` |
| StarPilot HEAD | `28ec3ccb80ff46fc88adbdf48e7b4a40c6afeede` |
| Tree | `cd13b2453cb3f46cf0573e440a4af663a09ae74b` |
| Branch | `bolt-starpilot-28ec3ccb` |
| Origin | self-contained `file:///data/starpilot-pins/28ec3ccb.git` |
| AGNOS | `19.6.2` |
| Services | `comma`, `ssh.socket`, `tailscaled` active |
| Disk | `/data` 4% used |

The official installer UI reached a Wayland initialization failure on the formatted unit. Recovery used the same official source/installer contract with a verified exact local pin and continuation handoff; the active SHA/tree now match that immutable pin.

## Settings projection

Owner-approved FireStar profile was restored and verified equal across `/data/params/d` and `/cache/params/d` for the selected keys:

- CEM ON; curve trigger OFF; lead/slower/stopped ON; stop time 7.7
- CSC ON
- Accel Standard, Decel Eco
- reverse cruise ON; ForceStops ON; ForceStandstill OFF
- manual `CHEVROLET_BOLT_CC_2017`
- OP longitudinal and GM Pedal longitudinal enabled
- AoL/advanced lateral ON
- NNFF, NNFFLite, TurnDesires, model randomizer OFF

Owner-only settings backup: `/data/boltev-recovery/28ec3ccb-firestar-settings-20260824T175925Z`.

The six `starpilot/assets/active_theme` worktree paths are runtime materialization and were preserved.

## Network recovery

StarPilot Galaxy's supported path installed Tailscale 1.102.3. `tailscaled.service` is enabled/active and state is under `/data/tailscale/state`. Restart readback preserved the new identity and IP. SSH port 22 accepts Daegyo's `daegyo-s23-for-c3x` key for user `comma`; Web UI port 8082 is reachable.

The pre-format identity `tizi-the-pond` / `100.71.169.100` had no recoverable state and remains history. Removing its stale tailnet record was not part of this work.

## Pending first ignition gate

At the canonical readback, `CarParams`, `CarParamsPersistent`, `CarParamsPrevRoute`, `FirmwareQueryDone`, `IsOffroad`, `IsOnroad`, and `IsEngaged` were absent. Therefore no claim is made yet for live fingerprint, Pedal detection, actuator delay/PID, Panda integration, or drive readiness.

Before driving/engaging:

1. owner powers the vehicle and allows firmware query to complete;
2. verify Bolt 2017 fingerprint, Pedal, OP longitudinal, `fwdCamera`, delay 0.6 and PID;
3. verify Panda/manager/UI/camera and no blocking alerts while offroad;
4. conduct a separately approved owner-driven low-speed first drive with manual braking readiness.

## Documentation canonicalization boundary

The 2026-08-25 repository update changes documentation only. It performs no C3X fetch, checkout, Params write, service restart, reboot, firmware operation, route mutation, or driving action.
